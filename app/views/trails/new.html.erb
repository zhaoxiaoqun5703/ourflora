<%= form_for(@trail) do |f| %>
  <% if @trail.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@trail.errors.count, "error") %> prohibited
        this species from being saved:
      </h2>

      <ul>
      <% @trail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if flash[:success] %>
    <div class="notice"><%= flash[:success] %></div>
  <% end %>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name%>
  </div>

  <div>
    <%= f.label :Information %>
    <%= f.text_area :information %>
  </div>

  <p><%= f.submit "Submit" %></p>

<% end %>
